use std::collections::LinkedList;

grammar;

/// single character

// let mut list = LinkedList::new()

pub Char:LinkedList<&'input str> = {
    <c:"."> <r:Char> => r.push_back(c),
    <c:".">          => mut LinkedList::from([c]),
    };
// pub Char: char = <c:r"."> => c.chars().next().unwrap();
